name: Transcript

on:
  workflow_dispatch:

jobs:
  bench:
    name: Bench Job
    runs-on: macos-14
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Execution
        run: |
          cp default.m4a ~
          PRODUCTION=false bash test.sh

